<div class="row">
    <div class="control-panel col-md-2 text-center">
        <!-- Usuario -->
        {% include 'GmaGameBundle:Layout:user.html.twig' %}
        <img src="{{ asset('game/assets/separator.png') }}" width="160">
        
        <!-- Tienda Virutal -->
        <div id="online_container" class="circle bg-red" data-toggle="popover" data-content="Quiero <b>canjear en la tienda</b> insignias por artículos que mejoren mi casa.">
            <span class="glyphicon glyphicon-shopping-cart"></span>
        </div>

        <!-- Jugar Online -->
        <div id="online_container" class="circle bg-green" data-toggle="popover" data-content="<b>Jugar con mis amigos.</b>">
            <span class="glyphicon glyphicon-globe"></span>
        </div>

        <!-- Ayuda -->
        <div id="help_container" class="circle bg-blue"  data-toggle="popover" data-content="Necesito más <b>ayuda</b>.">
            <a href="{{ url('gma_site_tips') }}" target="_blank">
                <span class="glyphicon glyphicon-question-sign"></span>
            </a>
        </div>  
        <img src="{{ asset('game/assets/separator.png') }}" width="160">
        <!-- Casa -->
        {% include 'GmaGameBundle:Layout:home.html.twig' %}
    </div>
</div>    
<script type="text/javascript">
    jQuery(document).ready(function($){
        var options = {container:'body',trigger:'hover', placement:'left', html:true};
        $('*[data-toggle="popover"]').popover(options);
    });
</script>